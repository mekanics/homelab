apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: rpi-alerts
  namespace: monitoring-system
  labels:
    release: kube-prometheus-stack
    app: kube-prometheus-stack
spec:
  groups:
    - name: apiserver
      rules:
        - alert: KubernetesAPIServerDown
          expr: up{job="apiserver"} == 0
          for: 5m
          labels:
            severity: critical
          annotations:
            summary: 'Kubernetes API Server is down'
            description: 'All Kubernetes API server endpoints have been unreachable for more than 5 minutes.'
