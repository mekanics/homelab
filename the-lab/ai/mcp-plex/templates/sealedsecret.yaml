# Placeholder — seal real values before deploying.
# Required keys: PLEX_TOKEN, PLEX_SERVER (optional if set in values.yaml env)
#
# To seal:
#   echo -n '<value>' | kubeseal --raw --from-file=/dev/stdin \
#     --namespace mcp-plex --name mcp-plex-secrets \
#     --controller-namespace sealed-secrets --cert <cert.pem>
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  creationTimestamp: null
  name: mcp-plex-secrets
  namespace: mcp-plex
spec:
  encryptedData: {}
  template:
    metadata:
      creationTimestamp: null
      name: mcp-plex-secrets
      namespace: mcp-plex
    type: Opaque
